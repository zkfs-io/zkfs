{
  "name": "@zkfs/contract-api",
  "version": "0.0.0",
  "license": "MIT",
  "public": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint ./src ./test",
    "test": "jest ./src ./test",
    "test:ci": "jest ./src ./test",
    "test:watch": "jest ./src ./test --watch",
    "prebuild": "npm run lint"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "snarkyjs": "^0.8.0"
  }
}

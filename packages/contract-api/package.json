{
  "name": "@zkfs/contract-api",
  "version": "0.0.4",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint ./src",
    "test": "jest ./src",
    "test:ci": "jest ./src",
    "test:watch": "jest ./src --watch",
    "prebuild": "npm run lint"
  },
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@types/lodash": "^4.14.191",
    "@zkfs/virtual-storage": "^0.0.1",
    "snarkyjs": "^0.8.0"
  }
}
